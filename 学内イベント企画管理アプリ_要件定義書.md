# 学内イベント企画管理アプリ（festaflow）要件定義書

## 概要
学内で複数の団体（クラス・部活など）が企画を出し合うイベント（文化祭、探究イベントなど）において、企画の提出・承認、予算・物品の管理、実行委員会から各団体への連絡を一元的に行うためのアプリ。単発のイベント専用ではなく、年をまたいで、また文化祭以外の複数企画イベントでも使い回せることを前提に設計する。

## 目的・背景
- 現状、企画・予算・備品・連絡の管理方法が定まっておらず（紙・口頭・LINEなど場当たり的）、参加団体数が30を超える規模になると破綻するリスクがある。
- 直近では2026年9月の文化祭で本格稼働させ、続く2026年11月8日の探究イベントでも使用する。将来的には、文化祭に限らず学内外の「複数企画が集まる場」全般で使えるインフラにしたい。
  - **タイムライン確定（2026-08-29ヒアリングで確定）**：「2026年9月の文化祭」は目前（来月）の本番であり、Phase 2はこの文化祭稼働に間に合わせる必要がある差し迫った締切。

## 対象ユーザー/受け手
- **団体側**：企画を提出する各クラス・団体。文化祭では30団体以上を想定。団体ごとの簡易ID認証でログインする。
- **管理側**：実行委員会など、企画を取りまとめる側。イベントごとに固定ID・パスワードでログインする（イベントが並行する場合、管理者アカウントもイベントごとに独立させる）。

## スコープ

### Phase 1（Must）— 実装済み・Supabase実データで動作確認・GitHub push済み
1. **企画提出フォーム**：初期項目は「企画名・内容・購入物品・予算・場所」。管理側はイベント中でも項目の追加・変更が可能。項目を後から追加した場合、既存の提出物にはその項目は「未入力」のまま残り、以降の新規提出から適用する。
2. **承認フロー**：管理側が各企画を承認／却下／差し戻しできる。
3. **予算管理（基本）**：実行委員会が団体ごとに予算をあらかじめ配分し、団体は企画提出時にその配分内で使用予定を入力する。配分を超える入力があった場合、提出自体をブロックする。
4. **連絡機能**：
   - 全団体への一斉連絡
   - 各企画に対する団体ごとの個別コメント
   - 未提出の団体への連絡（提出状況に応じたリマインドを含む）
   - 個別連絡・コメントには既読機能を付ける（全体連絡には既読機能は不要）
5. **ログイン**：団体側は団体ごとの簡易ID認証、管理側は固定ID・パスワード。
6. **端末対応**：スマートフォン・PCの両方で利用可能にする。

### Phase 2（2026年9月 文化祭 本格稼働まで＝目前）— Must・今回の実装対象
1. **物品管理の拡張（購入／借用の区別）**：
   - 物品ごとに「購入」または「借用」を選択して申請する。
   - 借用物品の在庫は、団体ごとではなく**学校全体で共有する単一プール**として管理する（例：折りたたみ椅子20脚を全団体で共有）。
   - 承認フローは購入・借用で分岐させる：
     - 購入物品は既存の承認／却下／差し戻しフローのみ。
     - 借用物品は上記に加えて**「在庫確保」の追加確認ステップ**を挟む（在庫が確保できて初めて確定状態になる）。
   - 複数団体の借用希望が同一物品でバッティングした場合、システムが自動で先着順に処理するのではなく、**管理側がその都度手動で裁定**する（誰にどれだけ割り振るかを決め、システムはその判断を記録・反映する）。
2. **外部データ連携（Google スプレッドシート）**：
   - 学校側が既存のGoogleスプレッドシートで在庫を管理しているため、アプリとスプレッドシート間で**双方向・常時自動同期**を行う（アプリでの借用確定がシートの残数に反映され、シート側の更新もアプリに反映される）。
   - **認証方式は未確定**：アプリ専用のGoogleサービスアカウントをシートの閲覧者として共有する方式は学校側の事情で採用が難しいことが判明しており、代替の認証方式（OAuthなど）を実装着手前に別途検討する（下記「技術面で未確定の事項」参照）。
   - 認証方式が固まるまでの間、アプリ内で在庫数を保持・編集できる機能を先に実装し、スプレッドシート連携は後続で追加する。
3. **規模拡張の検証**：30団体以上の規模で問題なく運用できることを確認する。

### Phase 2 では対象外・現状維持（今回のヒアリングで確定）
- **複数イベント同時稼働への対応**：2026年9月の文化祭稼働では不要と確定。今回も単一イベント運用のまま進め、データ設計（`events` テーブルを起点にevent_id単位で保持）はスキーマ変更なしで将来拡張できる状態だけ維持する。実際に複数イベントを同時運用可能にする実装は、必要になった時点で改めて着手する。

### Nice to have（優先度は低く、方向性の判断も現時点では不要と確定）
- **シフト作成機能**、**会計管理機能**：管理側と団体側の直接のやりとりを伴う機能ではないため、本来はスコープ外だが、「使うべきツールが定まっていない／使い方がわからない」領域として、将来的にこのアプリに含める余地がある。追加するかどうかの方向性判断は今回のタイミングでは不要（後回し）。

### Non-goals（意図的にやらないこと）
- 管理側と団体側のやりとりを伴わない機能全般（上記シフト・会計管理を除く）
- 実際の購入手続きそのもの（発注・決済処理など）
- 外部業者とのやり取り

## 主要な要件・機能（受け入れ基準の粒度）
1. 団体は、管理側が設定したフォーム項目に従って企画を提出できる。提出後、承認/却下/差し戻しの状態が団体側から確認できる。
2. 団体は、実行委員会から配分された予算の範囲内でのみ使用予定を入力・提出できる。超過時は提出がブロックされ、超過している旨が団体にわかる形で表示される。
3. 管理側は、全団体への一斉連絡・特定企画へのコメント・未提出団体への個別リマインドを送り分けられる。全体連絡以外は既読状況を管理側が確認できる。
4. （Phase 2）団体は、備品を「購入」と「借用」で区別して申請できる。借用は学校全体で共有する単一の在庫プールから引き当てられ、借用のみ承認とは別に「在庫確保」の確認ステップを経る。同一物品への希望がバッティングした場合は、管理側が手動で裁定した結果がシステムに反映される。
5. （将来）複数のイベントが同時に稼働する場合、各イベントの管理者・団体・予算・フォーム項目が独立して管理される（Phase 2時点では実装対象外。スキーマがevent_id単位である点のみ維持）。

## 制約
- Phase 1（探究イベント）：2026年11月8日
- Phase 2（文化祭 本格稼働）目標：**2026年9月**（目前の本番。「次年度」ではない）
- 対応端末：スマートフォン・PC両方
- ログインは学校支給アカウント等の既存基盤に依存せず、アプリ独自の簡易認証（団体側）／固定ID・パスワード（管理側）で完結させる

## 成功基準
- 管理側と団体側のコミュニケーションが、現状（紙・口頭・LINEなど）より円滑かつ迅速になっていること
- 企画の質が現状より落ちないこと（管理コストの増加が企画内容にしわ寄せされない）
- 「このアプリがなければ文化祭が回らない」と言えるレベルで、運用のインフラとして定着していること

## 未確定・要確認事項（2026-08-29ヒアリングで大半を確定。残るのは技術面のみ）
- ~~スプレッドシート連携の方向と頻度~~ → 確定：双方向・常時自動同期
- ~~借用物品がバッティングした場合の調整ルール~~ → 確定：管理側が都度裁定
- ~~Phase 2の「複数イベント同時稼働」の運用主体~~ → 確定：今回は対象外（単一イベント運用を維持）
- ~~シフト・会計機能を実際に追加するかどうかの最終判断時期~~ → 確定：現時点では判断不要（後回し）
- ~~プロダクトの正式名称~~ → 確定：festaflow
- **（新規・技術面）Googleスプレッドシートとの双方向同期をどう認証するか**：サービスアカウント共有が学校側の事情で難しいため、代替方式（OAuth等）を実装着手前にユーザーと相談する。
