# CLAUDE.md — festaflow（学内イベント企画管理アプリ）

## このファイルについて
このファイルは、要件ヒアリング（grill-me）を経て確定した要件を、Claude Codeでの実装に引き継ぐためのものです。「何を作るか」はここに確定した内容として書かれていますが、「どう作るか」（技術スタック、アーキテクチャ、データ設計など）のうち未確定の部分は必ずユーザーと相談してから実装してください。

## プロジェクト概要
学内で複数の団体（クラス・部活など）が企画を出し合うイベント（文化祭、探究イベントなど）において、企画の提出・承認、予算・物品の管理、実行委員会から各団体への連絡を一元的に行うアプリ。単発イベント専用ではなく、年をまたいで、また文化祭以外の複数企画イベントでも使い回せることを前提とする。

## 対象ユーザー
- **団体側**：企画を提出する各クラス・団体。文化祭では30団体以上を想定。団体ごとの簡易ID認証でログイン。
- **管理側**：実行委員会など、企画を取りまとめる側。イベントごとに固定ID・パスワードでログイン。

## Phase 1（実装済み・Supabase実データで動作確認・GitHub push済み）
- [x] 企画提出フォーム（初期項目：企画名・内容・購入物品・予算・場所。管理側がイベント中でも項目を追加/変更可能。項目追加時、既存提出はその項目「未入力」のまま、新規提出から適用）
- [x] 承認フロー（管理側が承認／却下／差し戻し）
- [x] 予算管理・基本（実行委員会が団体ごとに予算を事前配分。団体は配分内で使用予定を入力。超過時は提出をブロック）
- [x] 連絡機能（全体一斉連絡／企画ごとの個別コメント／未提出団体への連絡。個別連絡には既読機能、全体連絡には不要）
- [x] ログイン（団体側：簡易ID認証、管理側：固定ID・パスワード）
- [x] スマホ・PC両対応

## 今のフェーズ（Phase 2: 2026年9月 文化祭 本格稼働まで＝目前）で実装すべきこと（Must）
- [x] **物品管理の拡張**：物品ごとに「購入」／「借用」を選択。借用は学校全体で共有する**単一の在庫プール**から引き当てる。
- [x] **承認フローの分岐**：購入物品は既存の承認／却下／差し戻しのみ。借用物品はそれに加えて「在庫確保」の追加確認ステップを経る（未確定のまま承認しようとするとブロックされる）。
- [x] **バッティング時の裁定**：同一物品への借用希望が競合した場合、自動先着順ではなく**管理側が都度手動で裁定**し、その結果をシステムに反映する。
- [ ] **Googleスプレッドシート連携（双方向・常時同期）**：認証方式が未確定（下記参照）なため、まずアプリ内で在庫数を保持・編集できる機能を実装済み。連携部分は認証方式が決まってから追加する。
- [ ] 30団体以上の規模での動作確認

### Phase 2 で追加実装した項目（当初のMustに加え、実利用テスト・類似サービス調査から追加）
- [x] モバイル対応の見直し（ハンバーガーナビ、企画一覧のカード表示化、団体名・企画名の検索）
- [x] 借用物品選択時に在庫残数を表示（希望が競合しそうな場合は警告）
- [x] 管理側ダッシュボードの全体進捗バー、企画一覧からの「即承認」インライン操作
- [x] 団体の初回訪問時の案内、下書きの「最終更新」表示（保存への不安対策）
- [x] **提出締切の設定・カウントダウン表示**（`events.submission_deadline`、要マイグレーション適用）：文化祭実行委員は直前に締切超過が発生しやすいという実態調査に基づく
- [x] **管理側の統合受信箱**：全団体からの個別コメントを新着順に横断表示。直前に問い合わせが集中しても取りこぼさないため

## このフェーズではやらないこと（Non-goals / 後回し・対象外）
- **複数イベント同時稼働への対応**：2026年9月稼働では不要と確定。単一イベント運用のまま進め、データ設計（event_id単位）はスキーマ変更なしで将来拡張できる状態だけ維持する。実装自体は着手しない。
- シフト作成・会計管理機能 → 追加するかどうかの方向性判断も含め現時点では不要（後回し）
- 管理側・団体側のやりとりを伴わない機能全般
- 実際の購入手続き、外部業者とのやり取り

## 成功基準
- 管理側・団体側のコミュニケーションが、現状（紙・口頭・LINEなど）より円滑かつ迅速になること
- 企画の質が現状より落ちないこと
- 「このアプリがなければ文化祭が回らない」と言えるレベルで定着すること

## 技術面で未確定の事項（着手前に必ずユーザーと相談）
- **Googleスプレッドシートとの双方向・常時同期の認証方式**：アプリ専用のGoogleサービスアカウントをシートの閲覧者として共有する方式は学校側の事情で採用が難しいと判明済み。OAuthなど代替方式を検討する必要がある。
- 借用物品の「在庫確保」確認ステップの具体的な状態遷移（画面・DB設計）
- 複数イベント同時稼働を見据えた場合、Phase 2の物品管理まわりのデータ構造をどこまで将来拡張しやすくしておくか

## 詳しい要件
完全な要件定義書は `学内イベント企画管理アプリ_要件定義書.md` を参照。矛盾がある場合は要件定義書を正とする。

## 決定事項
- **プロダクト名**：festaflow（2026-08-29確定）
- 技術スタック（2026-08-28確定）：Next.js（App Router, TypeScript）+ Supabase（Postgres/Auth基盤として利用、認証自体は団体合言葉・管理者固定ID/PWのカスタム実装）。ホスティングはVercel + Supabaseを想定。
- データ設計（2026-08-28確定）：`events` テーブルを起点に全データをイベント単位（event_id）で保持する。複数イベント同時稼働はスキーマ変更なしで対応できるようにする（実装は対象外、設計のみ維持）。
- 在庫管理（2026-08-29確定）：在庫は団体ごとではなく学校全体で共有する単一プールとして管理する。
- 提出締切（2026-08-29追加）：`events`テーブルに`submission_deadline`（nullable）を追加。未設定時は既存動作のまま影響なし。`supabase/migrations/0003_submission_deadline.sql`をSupabase SQL Editorで適用する必要あり。
